apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
  name: "test5-ingress"
  namespace: "namespace-workflow-1743113596545"
  labels:
    app.kubernetes.io/name: test5-ingress
    app.kubernetes.io/part-of: test5-project
    kubernetes.azure.com/generator: draft
spec:
  ingressClassName: nginx.approuting.kubernetes.azure.com
  rules:
    - host: "contoso1.com"
      http:
        paths:
          - backend:
              service:
                name: "default"
                port:
                  number: 3000
            path: "/"
            pathType: Prefix