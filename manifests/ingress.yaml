apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
  name: "kenh-ingress"
  namespace: "namespace-workflow-1743188058673"
  labels:
    app.kubernetes.io/name: kenh-ingress
    app.kubernetes.io/part-of: kenh-project
    kubernetes.azure.com/generator: draft
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
    - host: "contoso.com"
      http:
        paths:
          - backend:
              service:
                name: "kenh"
                port:
                  number: 80
            path: "/"
            pathType: Prefix