apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
  name: "kenh-workflow-ingress"
  namespace: "namespace-workflow-1743184283472"
  labels:
    app.kubernetes.io/name: kenh-workflow-ingress
    app.kubernetes.io/part-of: kenh-workflow-project
    kubernetes.azure.com/generator: draft
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
    - host: "contoso.com"
      http:
        paths:
          - backend:
              service:
                name: "kenh-workflow"
                port:
                  number: 3000
            path: "/"
            pathType: Prefix