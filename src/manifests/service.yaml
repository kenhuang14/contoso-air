apiVersion: v1
kind: Service
metadata:
  name: kenh-flow
  namespace: default
  labels:
    app.kubernetes.io/name: kenh-flow
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kenh-flow
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000